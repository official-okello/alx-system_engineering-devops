# Creates a file 'school' in the /tmp directory with requirements:

file { 'school':
  path    => '/tmp/school',
  mode    => '0744',
  owner   => 'www-data',
  group   => 'www-data',
  content => 'I love Puppet',
}
